Mobile transaction filter sheet for NovaWallet wallet app for Zambia. A bottom-sheet modal sliding over a dimmed transaction history list. Sheet handle at top, title "Filter transactions", close X on the right. Section 1 "Type" with checkbox chips: All (active, indigo), Deposits, Withdrawals, Transfers, Fees. Section 2 "Status" with checkbox chips: All, Successful (green), Pending (amber), Failed (red). Section 3 "Date range" with segmented control: "Last 7 days", "Last 30 days" (active), "Custom". When Custom is active, show two date fields side by side: "From" (e.g., 01 Jul 2026) and "To" (e.g., 05 Aug 2026) as rounded input chips with calendar icons. Section 4 "Amount" with a min/max range: two inputs "Min ZMW" and "Max ZMW". Bottom of sheet: full-width pill buttons "Apply filters" (indigo, primary) and "Reset" (ghost). Show an active-filter count badge on the Apply button (e.g., "Apply (2)"). Style: clean light background, rounded, professional fintech, ZMW currency.
