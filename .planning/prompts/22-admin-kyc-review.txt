Admin KYC review screen (tablet/desktop-width) for NovaWallet platform back office. Top bar: "Admin Console" + admin avatar. Left: a list of pending KYC submissions (queue) — each item: avatar/initials, name, phone, tier requested, submitted date, a status dot; one selected (indigo highlight). Right (detail panel): applicant header (name, email, phone, NRC), requested tier "Standard" with required-docs checklist (National ID ✓, Selfie ✓, Proof of address ✗). A documents grid: thumbnails for National ID front/back, Selfie, Proof of address — each with a "View"/"Download" action and a status pill. Two primary actions: "Approve" (emerald) and "Reject" (red). A rejection reason textarea (shown as a card) with preset reasons ("Blurry document", "Name mismatch", "Address invalid"). Footer: "Reviewed by Admin User · all actions are audit-logged." Style: clean light background, rounded cards, professional admin, data-dense.